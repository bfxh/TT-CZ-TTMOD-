# 设计规范 · 资产库界面

现行规范。原则：**做成一台干练的工具，不做展示页**。所有决定都服务于
「在三万条资产里快速找到、并正确判断一个模型」。

## 一、色彩

一套灰阶 + **单一强调色**。颜色**只出现在「点」上**（6px 类型点 / 游戏点 / 状态点），
不铺面板、不做渐变、不做毛玻璃。

| 令牌 | 浅色 | 深色 | 用途 |
|---|---|---|---|
| `--bg` | `#EFEFF1` | `#151517` | 应用底 |
| `--panel` | `#FFFFFF` | `#1D1D20` | 面板 / 卡片 |
| `--panel-2` | `#F7F7F8` | `#232327` | 次级面（表头 / 缩略图底 / 代码块） |
| `--line` | `#E2E2E5` | `#323238` | 结构分隔线 |
| `--line-2` | `#EDEDEF` | `#292930` | 组内分隔线 |
| `--text` | `#191919` | `#E9E9EB` | 主文字 |
| `--text-2` | `#5F5F66` | `#9C9CA5` | 次要文字 |
| `--text-3` | `#98989F` | `#6C6C76` | 标签 / 占位 / 数字 |
| `--accent` | `#2B62D9` | `#5B8DEF` | 选中 / 当前排序 / 主按钮 |
| `--accent-soft` | `#EAF0FD` | `#1E2A44` | 选中底 |
| `--warn` | `#B26A00` | `#D08A2A` | 重复副本 |

类型色（仅作点，不做文字或底色的唯一信息载体）：

```
模块 #3D7BE0   武器 #C0503F   机甲 #B4791F   载具 #3E8FA8   建筑 #5A5AA8
地形 #8A7355   植被 #3E8A4E   道具 #B04A66   无人机 #3E9A96 特效 #A08A20
界面 #7A7A82   碰撞 #6A6A70   其他 #8E8E96
```

游戏色：泰拉科技 `#3E9B4F` · 战争机器人 `#C07A1E` · 重装上阵 `#8A5BD6`。

**对比度**：主文字对面板 ≥ 15:1，次要文字 ≥ 6:1，标签 ≥ 3.4:1，均满足 WCAG AA；
`--text-3` 不用于正文，只用于标签、单位、占位。

## 二、版面

```
48px 符号轨道 │ 52px 头部（品牌·搜索·视图·密度·排序·副本·统计·主题） │ 内容区 │ 24px 状态条
                                                              ↑ 筛选条（仅在有筛选时出现）
```

- 左轨道是**唯一允许纯符号**的区域（图标 18px，tooltip 补语义）
- 头部与排序菜单一律「图标 + 中文」，不做无字控件
- **没有右侧停靠栏**：详情一律走居中的总览面板（见下），列表区不再被挤窄
- 原来的「44 顶栏 + 32 排序条」两条堆叠已合并为**一条 52px 头部**，
  排序收进菜单，省下的 24px 纵向空间全给内容
- **筛选条**只在确有筛选时出现，把「现在被什么筛着」摆在明面上，每项可一键摘掉

### 详情总览面板（点卡片即开）

**占屏 92% × 92%**，四周留 4% 露出底下的网格；面板水平垂直都居中（实测左 66 / 右 66 / 上 34 / 下 34）。

```
┌───────────────── 48px 顶条：名称 · 分类 · 分组 · 面数      ⟵ ⟶ ★ │ 操作组 ✕ ─────────┐
│ ┌─ 左信息板 22% ─┐ ┌─────────── 中间 56% ───────────┐ ┌─ 右信息板 22% ─┐          │
│ │ 分类            │ │ ┌── 贴图排（中心上方）────────┐ │ │ 几何（含最长边/面顶点比）│          │
│ │ 标识（7 字段）   │ │ │ 原始材质 [t1][t2]… │平铺│亮度│ │ │ 关联（同游戏/类型/分组 │          │
│ │ 贴图（角色+清单）│ │ └───────────────────────────┘ │ │       /同名族/共用贴图）│          │
│ └────────────────┘ │        【 3D 模型 可拖动 】      │ │ 标记 / 索引             │          │
│   ╲ 外缘退远        │  HUD 左下        统计 右下       │ └────────────────┘          │
└─────────────── 遮罩：底下的网格可见但压暗 44% ───────────────────────────────────────┘
```

**两侧斜切：形状斜，像素不斜。**

这里踩过一个必须写进规范的坑：最早用真 3D 旋转（`rotateY(∓9deg) + rotateX(1.2deg)`）
做斜切，用户反馈「字模糊、细图标发虚」。做了合成对照实验（同一段文字，一份平放一份套变换，
量拉普拉斯方差占平放的百分比）：

| 实现 | 软件渲染 | GPU 光栅化 |
|---|---|---|
| 平放（对照） | 100.0% | 100.0% |
| `rotateY(7deg)` | **34.8%** | **35.4%** |
| `rotateY(7deg)` + `will-change:transform` | 34.8% | 35.4% |
| `rotateY(9deg) + rotateX(1.2deg)` | 30.2% | 30.5% |
| **`clip-path` 梯形** | **99.7%** | **99.6%** |

结论：**任何真 3D 旋转都要重采样，文字锐度直接掉三分之二**，`will-change` 不是主因
（第 2、3 行完全一样）。所以斜切只做**外形**——用 `clip-path` 切出梯形，
文字保持 1:1 像素，实测 99.7%，与平放同级。

```css
.pane-l{background:linear-gradient(to right, var(--panel) 84%, var(--panel-2));
  clip-path:polygon(0 0, 100% 0, calc(100% - 26px) 100%, 0 100%)}
.pane-r{background:linear-gradient(to left, var(--panel) 84%, var(--panel-2));
  clip-path:polygon(26px 0, 100% 0, 100% 100%, 0 100%)}
.pane-l .pane-in{padding-right:32px}   /* 斜切口留白，正文不进去 */
.pane-r .pane-in{padding-left:32px}
```

- 内缘斜切口 + 向内渐深的底色 ⇒ 读起来是一块「斜过来的板」，但内容是平的像素
- 斜切口宽度 26px，所以正文侧要留 32px，否则底部会被切掉
- 内层**不许再写 `will-change: transform`**：常驻合成层会锁住栅格化尺寸，静止时也发虚
- 遮罩层**不许用 `backdrop-filter`**：它建立 backdrop root，子树可能被按低分辨率栅格化
- 入场动画**只准动 `opacity`**：带 `scale` 的入场会让整个面板按缩放后的尺寸栅格化再放大

**贴图排（中心上方）**：模型自带贴图横向排开，**点缩略图即覆盖到模型上**。

| 控件 | 行为 |
|---|---|
| `原始材质` | 还原为默认灰模（撤掉覆盖） |
| 贴图缩略图 ×N | 点击即替换材质贴图，选中项打蓝框 + 右上角点 |
| `平铺 ×1/×2/×4` | 设 `RepeatWrapping` 与 `repeat.set(n,n)` |
| 亮度滑杆 0.3–1.6 | 乘在材质基色上，用于看清暗部细节 |

打开时默认挂上第一张贴图，随时可点别的覆盖或切回原始材质。

**视图开关不重置视角**：实体 / 线框 / 贴图 / 网格 / 自动旋转 / 重置视角
只改材质与网格辅助线，**不重新加载 OBJ、不动相机**（同一模型走增量重绘路径）。

#### 取景：按包围盒 8 角精确解，并让出 UI 占住的边带

两条硬约束，缺一条就会「模型被切掉」：

1. **UI 不许压在画布上**。贴图排一律走文档流（`flex:0 0 auto; margin:10px 12px 0`），
   画布独占剩下的整块 —— 绝对定位的浮条会盖住模型顶部，这不是观感问题而是功能性遮挡。
2. **相机距离要解出来，不能拍脑袋**。曾经的 `.position = c + (r*1.45, r*0.95, r*1.45)`
   对单位立方体给出张角 `asin(0.866/2.23)=22.8°`，而竖直半视角只有 20° —— 竖向上模型
   天生被切一截。现在的算法：

```
f  = (画布高/2) / tan(fov/2)            # 焦距（像素）
d >= a + |v|·画布高/(安全区高·tanV)      # 每个角都要落进安全区（竖直）
d >= a + |h|·画布高/(安全区宽·tanV)      # 同上（水平）
取 8 角的约束最大值 ×1.04 余量
再把投影内容的垂直中心对齐到安全区中心
```

- `a / h / v` = 该角在相机坐标架下的纵深 / 横向 / 竖向分量（坐标架与 three 的 `lookAt` 同约定）
- **安全区** = 画布减去 HUD（左下）与提示条（右上）占住的边带，由 `safeBand()` 实测得出
- 屏幕 y **向下为正**，所以投影取 `-f·v/depth`；符号写反会让补正翻倍、把模型推出安全区（踩过）
- 画布尺寸一变（窗口缩放 / 贴图排显隐）由 `ResizeObserver` 触发重新取景

实测（185 × 24 × 185 单位的地形板）：投影 `x 54~798 / y 143~498`，
安全区 `y 34~628`，**完整落在安全区内、远角超界 0**，占画布 87% 宽。


## 三、信息层级

分三层递进，**每一层都能独立回答一个问题**：

| 层级 | 回答 | 内容 |
|---|---|---|
| 卡片 | 一眼扫过 | 缩略图 / 名称 / ● 类型·分组 / 顶点·面数·贴图·大小 / ● 游戏 |
| 列表行 | 逐行比较 | 同上的横向展开，表头可点排序 |
| 总览面板 | 定案前核对 | 分类 3 值 · 标识 7 字段 · 贴图角色与清单 · 几何 8 项 · 关联 5 项 · 标记 · 索引 3 项 |

**关联**这一段是「超级详细」里最有用的一块：同游戏 / 同类型 / 同分组 / 同名族 /
共用首张贴图各有多少个 —— 决定能不能复用、会不会挑错版本。

### 卡片

```
┌──────────────────────┐
│      缩略图           │  ← 16:11，object-fit: contain，透明底渲染图
│              [⧉]     │  ← 重复副本角标（右上，warn 色）
├──────────────────────┤
│ 名称                  │  12.5px / 600
│ ● 类型 · 分组          │  11px / 点 6px
│ 顶点 20.3k  面数 40.4k│  11px，label --text-3 + 值等宽（--mono）
│ 贴图 6      大小 4.0 MB│
│ ─────────────────────│
│ ● 游戏名              │  10.5px / --text-3
└──────────────────────┘
```

指标只留**四项跨游戏可比的量**：顶点 / 面数 / 贴图 / 大小。
尺寸与包围盒单位不一致（三游戏差 30 倍），只在详情面板作为原始值展示，**不进卡片、不做排序**。

层级递进：卡片给四项 → 悬停给完整 title → 停靠面板给全字段 → 居中查看器给 3D。

## 四、组件

| 组件 | 规格 |
|---|---|
| 按钮 | 高 28px，圆角 5px，1px `--line` 描边，hover 换 `--panel-2` |
| 分段控件 | 外框 1px，内项无圆角，选中 `--accent-soft` + `--accent` 文字 |
| 输入框 | 高 28px，focus 时边框转 `--accent` 且底色转 `--panel` |
| 卡片 | 圆角 7px，hover 只换描边色，**不做位移与缩放** |
| 列表行 | 高 34px，网格列宽恒定，选中 `--accent-soft` 底 + `--accent` 描边 |
| 标签 chip | 高 20px，`--panel-2` 底 + 6px 色点，不整块着色 |
| 圆角 | 全局 7px（小元素 4-5px），不用 14px+ 大圆角 |
| 动效 | 只做 100-180ms 的底色/描边过渡；无入场动画、无 blur 动画 |
| 滚动条 | 11px，thumb `--text-3`，3px 透明边框内缩 |

## 五、键盘与无障碍

- `Ctrl K` 搜索 · `Ctrl 1/2` 切视图 · `←→` 在总览面板内连续翻模型 · `Esc` 逐层关闭 · 点卡片即开面板
- 所有纯符号按钮带 `title`（同时作为 tooltip 与无障碍名）
- 焦点可见：`:focus-visible` 统一 2px `--accent` 描边
- 尊重系统深浅色偏好由手动切换承担（本工具是长时间浏览器场景，不跟随系统以免突然闪白）

## 六、缩略图策略

**贴图 ≠ 模型**：贴图与模型是多对一，31,611 个模型只对应 2,942 张不同贴图，
最夸张一张被 502 个模型共用。所以缩略图改为 `build_thumbs.py` 逐个 OBJ 离线渲染：

- 正交等轴测 + Lambert + z-buffer，512 渲染 → 256 输出，透明底 WebP，约 5 KB/张
- **按类型上色**，缩略图自带分类语义，同时与卡片上的类型点呼应
- 渲染失败或空模型 → 回退为该类型的符号，不出破图

## 七、明确不做

背景渐变光晕、毛玻璃 `backdrop-filter`、卡片悬浮位移/缩放、彩色药丸整块着色、
大圆角、Emoji 当图标、纯符号表达语义、跨游戏的体积排序。

斜切是唯一的例外，但**只做外形**：用 `clip-path` 切出斜边，不做真 3D 旋转
（真旋转会把文字锐度砍到 35%，见 §二 的实测表）。

还有一条硬约定：**窄屏不许 `display:none` 掉头部控件**。曾经用媒体查询在 ≤1200px 藏密度、
≤1000px 藏副本/统计，结果就是用户报的「UI 图标消失」。现在搜索框先缩、头部可横向滚动，
控件一个不藏（实测 836~1416px 全部宽度下被隐藏数 = 0）。

同样明确不做的还有：**右侧停靠详情栏**。详情只在正中的总览面板里出现，
主列表永远拿满宽度。
